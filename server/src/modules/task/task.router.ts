import { Router } from "express";
import asyncHandler from "../../core/asyn-handler.js";

const taskRoutes = Router();


// router.post('/login')


// userRoutes.post('/user', asyncHandler(controller.create))



export default taskRoutes;